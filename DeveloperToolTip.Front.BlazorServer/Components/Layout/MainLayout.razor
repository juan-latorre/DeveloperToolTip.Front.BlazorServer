@inherits LayoutComponentBase
@inject Blazored.LocalStorage.ILocalStorageService LocalStorage

<RadzenComponents @rendermode="InteractiveServer" />

<div class="main-layout d-flex flex-column min-vh-100">

    <!-- Header -->
    <HeaderComponent></HeaderComponent>

    <!-- Body -->
    <div class="d-flex flex-grow-1">
        <main class="content flex-grow-1">
            @Body
        </main>
    </div>

    <!-- Barra de Accesibilidad -->
    <AccesibilidadComponent></AccesibilidadComponent>

    <!-- Footer -->
    <FooterComponent></FooterComponent>

</div>

@code {
    // protected override async Task OnAfterRenderAsync(bool firstRender)
    // {
    //     if (firstRender)
    //     {
    //         // Obtener el token del LocalStorage
    //         var authToken = await LocalStorage.GetItemAsync<string>("authToken");

    //         if (string.IsNullOrEmpty(authToken))
    //         {
    //             navManager.NavigateTo("/", true);
    //         }

    //         //var activeSession = await _IAuthService.GetActiveSession();

    //     }


    // }
}




